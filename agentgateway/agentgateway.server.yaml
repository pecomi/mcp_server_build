binds:
- port: 8081
  listeners:
  - routes:
    - policies:
        cors:
          allowOrigins:
          - "*"
          allowHeaders:
          - "*"
          exposeHeaders:
          - "Mcp-Session-Id"
      backends:
      - mcp:
          targets:
          - name: gongnuri-mcp-server
            mcp:
              host: http://localhost:8080/mcp